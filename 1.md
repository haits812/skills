Canvasは今は使えないので、**ここをそのまま「作業用の1枚もの」**としてまとめ直すね。以降はこれを基準に足したり削ったりすればいい。

# 取引先調査票AIツール ― ドキュメント整備メモ

## 0. 現状

ツール自体は**すでに動作済み**。  
現在は開発フェーズではなく、**後追いで成果物・テスト・運用方法を正式化するフェーズ**。

今回やることは、

> **実装済みツールを棚卸し → 要件に逆算 → 仕様化 → テスト → 業務運用確認 → 維持管理方法を文書化**

という流れ。

---

## 1. 最終成果物

基本的には、**利用マニュアル以外を1つのExcelブックにまとめる**。

| 成果物 | 目的 |
|---|---|
| 要件定義 | このツールで何を実現するか／何を保証するか |
| ツール仕様 | 現在のツールがどういう仕組みで動くか |
| システムテスト | AI・アルゴリズムとして期待する性能が出るか |
| UAT | 実際の業務フローとして使えるか |
| ナレッジ更新マニュアル | 回答・型・設問ナレッジをどう維持するか |
| 利用マニュアル | 一般利用者がどう操作するか ※別冊 |

---

# 2. Excelブック構成

```text
00_概要
01_要件定義
02_業務フロー
03_ツール仕様
04_AI処理仕様
05_システムテスト
06_UAT
07_ナレッジ更新
08_課題・制約
09_変更履歴
```

利用マニュアルだけ別途Word/PDF等で作成。

---

# 3. 要件定義

ここでは**どう作ったかではなく、何ができる必要があるか**を書く。

### 背景・目的

顧客から受領する取引先調査票は、

- Excel等のFMTが顧客ごとに異なる
- 1件〜100、200問程度まで設問数が変動する
- セキュリティ、サステナビリティ、法務、会社情報等、内容が多岐にわたる
- 設問単体のセルだけでは意味が成立せず、見出しや周辺セルの文脈が必要な場合がある
- 過去回答を再利用できる一方、新規・類似・曖昧な設問も存在する

ため、人手による読解・検索・回答作成に工数を要する。

本ツールでは、**調査票の設問を正規化し、既存ナレッジを利用して回答候補を提示することで、回答作成工数を削減する**。

### 主な機能要件

```text
調査票読込
↓
帳票構造・文脈を考慮した設問抽出
↓
設問単位への分解・正規化
↓
既存の型・過去設問との照合
↓
回答候補の取得
↓
一致しない／不明な場合は人間確認
↓
最終回答
```

特に要件として明記するものは、

**「無理に既存回答へ紐付けないこと」  
「判断不能を判断不能として返せること」  
「元設問と回答候補の対応を追跡できること」**

あたり。

---

# 4. 業務フロー

ここはシステムではなく、実際の仕事を書く。

```text
顧客から調査票受領
↓
ツールへ投入
↓
設問分解・正規化
↓
既存ナレッジとの照合
↓
回答候補生成
↓
担当者確認
↓
必要に応じて関係部署確認
↓
回答確定
↓
顧客へ返却
↓
新規・修正回答をナレッジへ反映
```

UATは基本、この流れをそのままテストする。

---

# 5. ツール仕様

ここは**現在すでに動いているものを、そのまま書き起こす**。

現状イメージは、

```text
顧客調査票
      ↓
skill / script
帳票構造・セル情報等を取得
      ↓
LLM
周辺文脈を含めて設問を分解・正規化
      ↓
正規化設問一覧
      ↓
script
約400～500の型から近い候補を抽出
      ↓
Top N候補
      ↓
候補型に紐づく過去設問を取得
      ↓
LLM
新規設問と既存設問の意味を最終照合
      ↓
既存回答候補
または
判断不能／人間確認
```

ここではさらに、

- skillの役割
- scriptの役割
- LLMの役割
- 入力データ
- 中間データ
- 正規化後の形式
- 型マスター
- 過去設問との紐付け
- 候補抽出方法
- LLM最終判定
- エラー時処理
- ログ
- 出力

を書く。

---

# 6. システムテスト

ここは普通の画面テストというより、**AIアルゴリズム評価**として扱う。

目的は、

> 「一回当たった」ではなく、  
> **入力が変わっても一定の精度・安定性で機能するかを評価すること。**

主な評価対象は、

| 評価箇所 | 確認内容 |
|---|---|
| 設問分解 | 人間と同じ単位で質問を切り出せるか |
| 文脈復元 | 見出し・周辺セルを適切に反映できるか |
| 正規化 | 元質問の意味を変えていないか |
| 型候補抽出 | 正解型がTop N候補に入るか |
| 最終LLM判定 | 候補から正しい型・既存設問を選択できるか |
| 該当なし判定 | 無関係な型へ無理やり分類しないか |
| 回答照合 | 正しい回答候補へ紐付けられるか |
| 安定性 | 同一・類似入力で極端に結果が揺れないか |
| コスト | 1帳票／100問当たりのAI利用コスト |
| 処理時間 | 実業務上許容可能か |

### テストデータ

理想的には、

**Golden Set**  
人間が正解を確定した過去帳票。

**Challenge Set**  
結合セル、離れた見出し、英語、曖昧表現、類似型など、わざと難しいもの。

**Holdout Set**  
開発・調整では使っていない帳票。最終評価用。

### 特に見る指標

例えば、

```text
設問分解成功率
正規化妥当率
Top N Recall
最終型判定精度
誤った回答への紐付け率
判断不能として止められた率
人間による修正率
100問当たりコスト
```

ここは今後、実測値を入れながら合格基準を決める。

---

# 7. UAT

UATは**アルゴリズム精度ではなく、業務として回るか**を見る。

実際の利用者が、

```text
調査票受領
↓
ツール投入
↓
結果確認
↓
修正
↓
必要なら関係部署へ確認
↓
回答確定
↓
顧客返却
↓
ナレッジ更新
```

まで一連で実施。

確認するのは、

- 操作方法が分かるか
- AI結果の確認ポイントが分かるか
- 間違った結果を修正できるか
- ナレッジにない設問への対応ができるか
- 誰に確認すべきか分かるか
- 従来より業務工数が削減されるか
- 回答後のナレッジ更新まで運用できるか

という**業務成立性**。

---

# 8. ナレッジ更新

ここはツールを今後腐らせないための運用。

対象は主に、

```text
型
過去設問
承認済み回答
回答根拠
関連情報
```

新しい設問が来た場合は、

```text
新規設問
↓
既存型で表現可能か
├─ YES → 既存型へ設問例として追加
└─ NO  → 新規型の必要性を検討
```

回答内容が変わった場合は、

```text
回答変更
↓
関連する型・過去設問を確認
↓
新回答へ更新
↓
旧回答の扱いを明確化
↓
影響範囲を再テスト
```

特に決める必要があるのは、

- 型追加基準
- 型統合基準
- ID／名称規則
- 過去設問追加方法
- 回答更新方法
- 承認方法
- 古い回答の無効化方法
- 更新後の回帰テスト

---

# 9. 利用マニュアル

これは最後に作る。

利用者に必要なのは基本、

```text
どう投入するか
↓
どう実行するか
↓
結果のどこを見るか
↓
何を修正するか
↓
エラー時どうするか
↓
どう回答を完成させるか
```

だけ。

内部の400〜500型やLLMロジックは基本書かない。

---

# 10. 今からやる順番

**① 現在のツール仕様を書き起こす  
→ ② そこから要件を逆算する  
→ ③ 業務フローを確定する  
→ ④ システムテストの評価項目・データ・基準を決める  
→ ⑤ UATシナリオを作る  
→ ⑥ ナレッジ更新方法を文書化する  
→ ⑦ 最後に利用マニュアルを作る**

今は要するに、**「新しく設計する」のではなく、「すでに作ったものを正式な成果物へ変換する」**のが仕事。

これを親メモにして進めれば、話が散らからないはず。